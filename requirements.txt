# Everything here is free and runs locally. No paid account, no API key.
# Versions are the ones this repository was developed and verified against.

# RAG core
chromadb>=1.0,<2.0
sentence-transformers>=3.0            # primary embedding backend
scikit-learn>=1.4                     # deterministic offline fallback embedder
numpy>=1.26

# orchestration and review
crewai>=1.0,<2.0
autogen-agentchat>=0.7,<0.8
autogen-core>=0.7,<0.8
langchain-core>=0.3                   # session memory

# deployment
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27                           # fastapi.testclient
pydantic>=2.7
python-dotenv>=1.0
